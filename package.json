{
  "name": "trend-haircuts",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "validate": "node scripts/validate_articles.cjs",
    "build": "cp index.source.html index.html && npm run validate && vite build && cp dist/index.html index.html && cp -r dist/assets/* assets/",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
